Flask>=2.3.0
google-generativeai>=0.8.6
# Optional: newer client. Install if you plan to migrate to the newer package
# pip install google-genai
google-genai>=0.0.1 ; extra == "genai"
python-dotenv>=1.0.0
